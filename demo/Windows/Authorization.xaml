<Window x:Class="demo.Windows.Authorization"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:demo.Windows"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        Title="Авторизация" Height="340" Width="580" MinWidth="480" MinHeight="280">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Padding="32" VerticalAlignment="Stretch">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Demo" FontSize="28" HorizontalAlignment="Center"/>
                <TextBlock Text="Логотип" FontSize="14" Margin="0,8,0,0" HorizontalAlignment="Center" Opacity="0.85"/>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" Padding="32" VerticalAlignment="Stretch">
            <StackPanel VerticalAlignment="Center" MaxWidth="288">
                <TextBlock Text="Логин" Margin="0,0,0,4"/>
                <TextBox Name="BoxLogin" Padding="6,4" Margin="0,0,0,16" Text="root"/>
                <TextBlock Text="Пароль" Margin="0,0,0,4"/>
                <TextBox Name="BoxPassword" Padding="6,4" Margin="0,0,0,16" Text="rootrootroot"/>
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <Button Content="Войти" Padding="12,6" MinWidth="96" Click="Button_authorization"/>
                    <Button Content="Войти как гость" Padding="12,6" Margin="8,0,0,0" MinWidth="140" Click="Button_authorization_gouest"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>
