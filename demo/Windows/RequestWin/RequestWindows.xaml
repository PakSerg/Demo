<Window x:Class="demo.Windows.RequestWin.RequestWindows"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:demo.Windows.RequestWin"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        Title="Заявки" Height="600" Width="830" MinWidth="600" MinHeight="400">
    <DockPanel Margin="16" LastChildFill="True">
        <DockPanel DockPanel.Dock="Top" Margin="0,0,0,16">
            <Button Content="Назад" Padding="10,6" MinWidth="88" Click="Button_exit" DockPanel.Dock="Left"/>
            <StackPanel/>
        </DockPanel>
        <StackPanel Name="PanelBottomButton" Orientation="Horizontal" DockPanel.Dock="Bottom" Margin="0,16,0,0">
            <Button Content="Добавить" Width="88" Padding="10,6" Margin="0,0,8,0" Click="Button_add_reques"/>
            <Button Content="Удалить" Width="88" Padding="10,6" Click="Buutton_delite_reques"/>
        </StackPanel>
        <ListBox Name="BoxOrder" Padding="4" MinHeight="300" SnapsToDevicePixels="True">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <DockPanel Height="100" Margin="0,0,0,8" SnapsToDevicePixels="True">
                        <Border DockPanel.Dock="Right"
                                BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"
                                BorderThickness="1"
                                Margin="8,0,0,0"
                                Padding="8"
                                MinWidth="120">
                            <TextBlock Text="{Binding DeliveryDate, StringFormat='dd.MM.yyyy'}"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Center"/>
                        </Border>
                        <Border BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"
                                BorderThickness="1"
                                Margin="0,0,8,0"
                                Padding="8"
                                Width="640">
                            <StackPanel>
                                <TextBlock Text="{Binding Code}" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Status.Status1}"/>
                                <TextBlock Text="{Binding PickupPoint.Adress}" />
                                <TextBlock Text="{Binding OrderDate, StringFormat='dd.MM.yyyy'}"/>
                            </StackPanel>
                        </Border>
                    </DockPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </DockPanel>
</Window>
