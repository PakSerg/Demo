<Window x:Class="demo.Windows.Main"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:demo.Windows"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        Title="Приложение" Height="850" Width="1300" MinWidth="800" MinHeight="500">
    <DockPanel Margin="16" LastChildFill="True">
        <DockPanel DockPanel.Dock="Top" Margin="0,0,0,16">
            <Button Content="Выход" Padding="10,6" MinWidth="88" Click="Button_exit_user" DockPanel.Dock="Left"/>
            <TextBlock DockPanel.Dock="Right" VerticalAlignment="Center" Margin="16,0,0,0">
                <Run Text="Добро пожаловать: "/>
                <Run Name="BoxUserName"/>
            </TextBlock>
            <StackPanel/>
        </DockPanel>
        <StackPanel Name="PanelBottomButton" Orientation="Horizontal" DockPanel.Dock="Bottom" Margin="0,16,0,0">
            <StackPanel Name="PanelBottomAdmin" Orientation="Horizontal">
                <Button Content="Добавить" Width="88" Padding="10,6" Margin="0,0,8,0" Click="Button_add_product"/>
                <Button Content="Удалить" Width="88" Padding="10,6" Margin="0,0,8,0" Click="Buutton_delite_product"/>
            </StackPanel>
        </StackPanel>
        <DockPanel>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8" Name="PanelFind">
                <TextBox Width="120" Name="BoxFind" Padding="6,4" Margin="0,0,8,0" TextChanged="BoxFind_TextChanged"/>
                <TextBlock Text="Поиск" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox Name="ComboBoxItem" SelectedIndex="0" Width="160" Padding="6,4" Margin="0,0,8,0" SelectionChanged="ComboSuppliers_SelectionChanged"/>
                <RadioButton Content="по возрастанию" Margin="0,0,8,0"
                             Checked="RadioUpp_Checked"
                             IsChecked="True" VerticalAlignment="Center"/>
                <RadioButton Content="по убыванию"
                             Checked="RadioUpp_Checked" VerticalAlignment="Center"/>
            </StackPanel>
            <ListBox Name="BoxProduct" Margin="0,8,0,0" MinHeight="400" Padding="4"/>
        </DockPanel>
    </DockPanel>
</Window>
